import { LchRange } from "./types";

export const L_RANGE: LchRange = {
  min: 0,
  max: 100,
};

export const C_RANGE: LchRange = {
  min: 0,
  max: 140,
};

export const H_RANGE: LchRange = {
  min: 0,
  max: 360,
};
